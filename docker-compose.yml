version: '3.8'

services:
  # Servidor Web & Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: casa_de_lio_nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web
    networks:
      - rede_casa_de_lio

  # Aplicação FastAPI
  web:
    build: .
    container_name: casa_de_lio_web
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Sobrescreve endpoint S3 para comunicação interna da rede docker
      - S3_ENDPOINT_URL=http://minio:9000
    volumes:
      - ./banco.db:/app/banco.db
      - ./uploads:/app/uploads
    expose:
      - "8000"
    depends_on:
      - minio
    networks:
      - rede_casa_de_lio

  # Armazenamento de Objetos MinIO (Compatível com AWS S3)
  minio:
    image: minio/minio:latest
    container_name: casa_de_lio_minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-minioadmin}
    ports:
      - "9000:9000"   # API S3
      - "9001:9001"   # Painel Web MinIO
    volumes:
      - minio_data:/data
    networks:
      - rede_casa_de_lio

  # Inicializador automático para criar o bucket no MinIO na primeira execução
  minio-createbuckets:
    image: minio/mc:latest
    container_name: casa_de_lio_minio_setup
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 minioadmin minioadmin;
      /usr/bin/mc mb --ignore-existing myminio/casa-de-lio-uploads;
      exit 0;
      "
    networks:
      - rede_casa_de_lio

volumes:
  minio_data:

networks:
  rede_casa_de_lio:
    driver: bridge
